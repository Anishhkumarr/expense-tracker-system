services:
  - type: web
    name: expense-tracker-backend
    env: java
    rootDir: expense-tracker-system/backend
    buildCommand: mvn clean package -DskipTests
    startCommand: java -jar target/*.jar
